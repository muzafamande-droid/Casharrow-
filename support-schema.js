const db=require('./database-pg');
async function ensureSupportSchema(){await db.query(`CREATE TABLE IF NOT EXISTS support_tickets (id BIGINT PRIMARY KEY,user_id BIGINT NOT NULL REFERENCES users(id) ON DELETE CASCADE,category TEXT NOT NULL,subject TEXT NOT NULL,status TEXT NOT NULL DEFAULT 'open',created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW());CREATE TABLE IF NOT EXISTS support_messages (id BIGINT PRIMARY KEY,ticket_id BIGINT NOT NULL REFERENCES support_tickets(id) ON DELETE CASCADE,sender_user_id BIGINT NOT NULL REFERENCES users(id) ON DELETE CASCADE,sender_role TEXT NOT NULL,message TEXT NOT NULL,created_at TIMESTAMPTZ NOT NULL DEFAULT NOW());CREATE SEQUENCE IF NOT EXISTS support_tickets_id_seq;CREATE SEQUENCE IF NOT EXISTS support_messages_id_seq;CREATE INDEX IF NOT EXISTS idx_support_tickets_user ON support_tickets(user_id,updated_at DESC);CREATE INDEX IF NOT EXISTS idx_support_messages_ticket ON support_messages(ticket_id,id);`);await db.query("SELECT setval('support_tickets_id_seq',COALESCE((SELECT MAX(id) FROM support_tickets),0),true)");await db.query("SELECT setval('support_messages_id_seq',COALESCE((SELECT MAX(id) FROM support_messages),0),true)");}
module.exports={ensureSupportSchema};